#Notas:
primer programa node
```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     la base de la tabla de multiplicar          [number] [required]
  -l, --lista    muestra la tabla de multiplicar      [boolean] [default: false]
  -h, --hasta    hasta que numero vamos hacer la lista    [number] [default: 10]
```